# cmd.exe 는 배치 파일이 윈도우 줄바꿈(CRLF)일 것으로 보고 읽는다.
# 유닉스 줄바꿈(LF)만 있으면 여러 줄짜리 `if ( ... )` 나 `for ... do ( ... )`
# 안에서 엉뚱하게 끊길 수 있다. `8_fix_gpu.bat` 이 바로 그 모양이다.
#
# 지금까지는 개발 PC 의 git 설정(`core.autocrlf`)이 알아서 바꿔 줘서 굴러갔다.
# 그 설정에 기대지 않고 여기서 못을 박는다. 어느 컴퓨터에서 받아도 배치
# 파일은 CRLF 로 놓인다.
*.bat text eol=crlf
